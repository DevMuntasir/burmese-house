import { address, deliveryOption, homeSection, orderItem, paymentDetails, productVariant, seo, specification } from "./objects";
import { banner, category, customer, homepageSettings, order, paymentSettings, product, shippingSettings, storeSettings } from "./documents";

export const schemaTypes = [seo, address, deliveryOption, productVariant, orderItem, paymentDetails, specification, homeSection, product, category, order, customer, banner, storeSettings, shippingSettings, paymentSettings, homepageSettings];
